= turbo_frame_tag place_block do
  = bootstrap_form_with(url: admin_event_place_blocks_url, data: { action: "turbo:submit-end->modal#close"}) do |form|
    = form.text_field :name
    = form.number_field :capacity
    = form.primary
