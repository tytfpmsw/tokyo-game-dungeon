.breadcrumb
  %li.breadcrumb-item.active
    出展者トップページ

.p-exhibitor-events-index
  %h2.mt-4.mb-4 出展情報提出可能イベント一覧

  .row.justify-content-center
    - if @events.present?
      %table.col-8.mg-auto
        = render @events
    - else
      %p 現在、出展情報を提出できるイベントはありません。