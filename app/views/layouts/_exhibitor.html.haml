.exhibitor-wrapper.container 
  - if exhibitor_signed_in?
    .row 
      .col-4 
        = render 'exhibitor/shared/sidemenu' 
      .col-8
        = yield
  - else
    .row
      .col-12
        = yield